.common-rule:
  rules:
    - if: ($CI_PIPELINE_SOURCE == 'merge_request_event' || $CI_COMMIT_BRANCH == 'SYSPRI-198-containerize-core')

.after-merge-rule:
  rules:
    - if: $CI_COMMIT_BRANCH == 'main'
